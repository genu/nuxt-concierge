// Stub — Task 5 replaces this file with the real in-process driver.
import type { ConciergeDriver } from './types'

export const createMemoryDriver = (): ConciergeDriver => {
  throw new Error('[nuxt-concierge] memory driver not implemented yet')
}
